<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kopo.poly.jolljack.mapper.IFindMapper">

    <select id="getUserByNameAndEmail" parameterType="UserDTO" resultType="UserDTO">
        SELECT user_id,
               login_id,
               name,
               email,
               status
        FROM users
        WHERE name = #{name}
          AND email = #{email}
          AND status = 'ACTIVE'
            LIMIT 1
    </select>

    <select id="getUserByLoginIdAndNameAndEmail" parameterType="UserDTO" resultType="UserDTO">
        SELECT user_id,
               login_id,
               name,
               email,
               password_hash,
               status
        FROM users
        WHERE login_id = #{loginId}
          AND name = #{name}
          AND email = #{email}
          AND status = 'ACTIVE'
            LIMIT 1
    </select>

    <update id="updatePasswordHash" parameterType="UserDTO">
        UPDATE users
        SET password_hash = #{passwordHash},
            updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="getUserByUserId" parameterType="UserDTO" resultType="UserDTO">
        SELECT user_id,
               password_hash
        FROM users
        WHERE user_id = #{userId}
          AND status = 'ACTIVE'
            LIMIT 1
    </select>

</mapper>