<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kopo.poly.jolljack.mapper.ITradeMapper">

    <select id="getTradePostCount" parameterType="TradeSearchDTO" resultType="Long">
        SELECT COUNT(*)
        FROM trade_post
        WHERE region_id = #{regionId}
          AND status != 'DELETED'
    </select>


    <select id="getTradePostList" parameterType="TradeSearchDTO" resultType="TradePostListDTO">
        SELECT trade_post_id,
               region_id,
               title,
               price,
               quantity,
               image_key,
               status,
               created_at
        FROM trade_post
        WHERE region_id = #{regionId}
          AND status != 'DELETED'
        ORDER BY created_at DESC
            LIMIT #{pageSize}
        OFFSET #{offset}
    </select>

</mapper>